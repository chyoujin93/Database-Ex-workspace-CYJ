-- ## 5. 기본 문법 및 예시

-- ### 🔹 열에 별칭 지정 (AS 사용)
-- ENAME 컬럼을 "사원 이름"이라는 이름으로 출력
SELECT ENAME FROM EMP;
SELECT ENAME AS "사원 이름" FROM EMP;
-- 공백이 들어가면 큰따옴표 무조건 사용(AS는 여전히 생략 가능-가독성 목적)
-- 공백 없으면 AS/큰따옴표 없이도 가능

-- ### 🔹 연산식에 별칭 지정
-- > 연봉 계산 후 “연봉  상여금 미포함”라는 이름으로 출력
SELECT ENAME AS "사원이름", SAL * 12 AS "연봉 상여금 미포함" FROM EMP;


-- ### 🔹 AS 없이 별칭 지정
-- 큰따옴표는 공백 있을 경우, 무조건 사용! 없다면 없어도 가능.
SELECT ENAME "직 원", JOB "직 무" FROM EMP;